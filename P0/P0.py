y=10
x=13
k=(y, x, 13)
print(min(k))
{
lista = [x, y, z]
datos = [a, b, c]
M = np.matrix([lista, datos])
suma = sum(lista)
maximo = max(lista)
minimo = min(lista)
ecuacion = ((suma * maximo) + (minimo**3))
estadistica = describe(lista)
s = np.arange(1, 20, 3)
aleatorio = rand(100)
}
